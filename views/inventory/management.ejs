<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | Inventory Management
    </title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        /* Base styles from user prompt */
        body {
            font-family: Arial, sans-serif;
            background: #f5f6f7;
            margin: 0;
            padding: 0;
        }

        main {
            max-width: 800px;
            margin: 3rem auto;
            background: #fff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            margin-bottom: 1.5rem;
            color: #333;
        }

        .flash-message {
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1rem;
            font-weight: bold;
        }

        .flash-message.notice {
            background: #e6ffed;
            border: 1px solid #b2f5ea;
            color: #065f46;
        }

        .flash-message.error {
            background: #ffe6e6;
            border: 1px solid #ffb3b3;
            color: #7f1d1d;
        }

        /* Updated styles for the modern-looking buttons */
        .management-links {
            margin-top: 2rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .management-links a {
            display: block;
            padding: 0.75rem 1rem;
            text-decoration: none;
            font-weight: bold;
            border-radius: 8px;
            text-align: center;
            transition: background-color 0.3s ease;
        }

        /* Utility classes for styling the new links (mimicking Tailwind classes) */
        .bg-blue-500 {
            background-color: #3b82f6;
        }

        .hover\:bg-blue-700:hover {
            background-color: #1d4ed8;
        }

        .bg-indigo-500 {
            background-color: #6366f1;
        }

        .hover\:bg-indigo-700:hover {
            background-color: #4338ca;
        }

        .text-white {
            color: white;
        }

        .shadow-lg {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }

        .mt-8 {
            margin-top: 2rem;
        }

        .mb-4 {
            margin-bottom: 1rem;
        }

        .text-2xl {
            font-size: 1.5rem;
        }

        .font-semibold {
            font-weight: 600;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
        }

        .form-group select {
            padding: 0.5rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 100%;
        }

        /* Table styling for Task 4 */
        #inventoryDisplay table {
            width: 100%;
            border-collapse: collapse;
        }

        #inventoryDisplay th,
        #inventoryDisplay td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        #inventoryDisplay th {
            background-color: #f2f2f2;
        }

        #inventoryDisplay tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        #inventoryDisplay a {
            color: #3b82f6;
            text-decoration: none;
        }
    </style>
    

</head>

<body>
    <main>
        <% if (title) { %>
            <h1>
                <%= title %>
            </h1>
            <% } %>

                
                <!-- FIXED FLASH MESSAGES BLOCK: Shows all notice and error messages -->
                <% if (messages) { %>
                    <% Object.keys(messages).forEach(type=> { %>
                        <% messages[type].forEach(msg=> { %>
                            <div class="flash-message <%= type %>">
                                <%= msg %>
                            </div>
                            <% }) %>
                                <% }) %>
                                    <% } %>
                                        <!-- END FLASH MESSAGES -->

                                        <h2 class="text-2xl font-semibold mb-4">Classification Management</h2>
                                        <p class="mb-6">Use the links below to create new classifications and inventory
                                            items.</p>

                                        <div class="management-links">
                                            <!-- Task One Requirement: Link to Add New Classification View -->
                                            <a href="/inv/add-classification"
                                                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg shadow-lg">
                                                Add New Classification
                                            </a>

                                            <!-- Task One Requirement: Link to Add New Inventory Item View -->
                                            <a href="/inv/add-inventory"
                                                class="bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded-lg shadow-lg">
                                                Add New Inventory Item
                                            </a>
                                        </div>

                                        <!-- Task 4: Inventory Item Management Section -->

                                        <h2 class="text-2xl font-semibold mt-8 mb-4">Manage Inventory Items</h2>
                                        <!-- Updated paragraph instruction -->
                                        <p class="mb-4">Select a classification from the list to see the items belonging
                                            to that classification.</p>

                                        <!-- Classification Select Dropdown (Passed from invController) -->
                                        <div class="form-group mb-4">
                                            <label for="classificationList">Inventory by Classification:</label>
                                            <%- classificationList %>
                                        </div>

                                        <!-- Placeholder for the Inventory Table (Now an empty <table> element) -->
                                        <table id="inventoryDisplay"></table>

                                        <!-- NoScript element added -->
                                        <noscript>JavaScript must be enabled to use this page.</noscript>

                                        <!-- Task 4: Client-side JavaScript to handle the dropdown selection -->
                                        <!-- Ensure this file exists at /public/js/inventory.js -->
                                        <script src="/js/inventory.js"></script>

    </main>
    

</body>

</html>